# 启灵 S4 机器人多相机手眼标定实验报告

**实验日期：** 2026 年 7 月 31 日至 8 月 3 日  
**实验平台：** 启灵 S4 机器人双臂与头部相机  
**相机型号：** 左手 D405、右手 D405、头部 D435  
**标定类型：** 眼在手上（Eye-in-Hand）与眼在手外（Eye-to-Hand）  
**左手工具坐标系：** `LH_hand_base_link`  
**右手工具坐标系：** `RH_hand_base_link`  
**相机光学坐标系：** `camera_color_optical_frame`

**结果文件：**

- 右手 D405：`samples/20260731_150143/eye_in_hand_result_filtered.yaml`
- 左手 D405：`samples/20260803_094251/left_eye_in_hand_filtered.yaml`
- 头部 D435：`samples/left_eye_to_hand/20260803_135418/left_eye_to_hand.yaml`
- 头部 D435 验证：`validation_samples/left_eye_to_hand/20260803_145850/eye_to_hand_validation_report.yaml`

---

## 摘要

本实验对启灵 S4 机器人的右手 D405、左手 D405 和头部 D435 开展手眼标定。左右手相机随机器人末端运动，采用眼在手上模型求解工具坐标系到相机光学坐标系的变换 `T_tool_camera`；头部相机固定在机器人本体，AprilTag 标定板刚性固定在左手，采用眼在手外模型求解 `T_base_camera` 和 `T_tool_tag`。

实验使用 `tag36h11` 家族、ID 为 10、边长为 0.107 m 的 AprilTag。通过 Xbox 手柄拖动示教改变机器人位姿，在每个位姿下同步记录关节角、Pinocchio 正运动学位姿、AprilTag 视觉位姿和带标注的彩色图像。所有外参均通过 SE(3) 闭环残差和鲁棒非线性最小二乘求解。

右手 D405 在 15 组有效样本上的内部闭环平移 RMSE 为 8.684 mm，旋转 RMSE 为 1.247°；10 组新数据的独立评估结果为 20.798 mm / 1.677°。左手 D405 在 15 组有效样本上的内部闭环结果为 13.397 mm / 1.995°。头部 D435 使用全部 17 组标定数据，内部闭环结果为 6.500 mm / 1.211°；另外 10 组新位姿相对原外参的验证结果为 21.775 mm / 2.520°。

**关键词：** 启灵 S4；手眼标定；眼在手上；眼在手外；RealSense D405；RealSense D435；AprilTag；Pinocchio；SE(3)

---

## 1 引言

### 1.1 实验背景

在机器人视觉定位、目标抓取和视觉伺服任务中，相机获得的目标位姿通常表达在相机坐标系中，而机器人控制和运动规划需要在机器人基座坐标系或末端工具坐标系中使用这些信息。因此，必须建立相机坐标系与机器人坐标系之间的准确刚体变换。

左右手 D405 与手部保持固定安装关系，相机随末端共同运动，属于典型眼在手上结构。头部 D435 与机器人基座保持固定关系，标定板随左手运动，属于眼在手外结构。

### 1.2 实验目的

1. 求取右手 `RH_hand_base_link` 到右手 D405 光学坐标系的 `T_tool_camera`。
2. 求取左手 `LH_hand_base_link` 到左手 D405 光学坐标系的 `T_tool_camera`。
3. 求取机器人 `base_link` 到头部 D435 光学坐标系的 `T_base_camera`。
4. 建立拖动示教、正运动学、AprilTag 检测和 SE(3) 外参优化的完整实验流程。
5. 检查 SDK 关节角到 URDF 广义坐标的有效映射，并分析闭环一致性误差。

---

## 2 方法

### 2.1 实验系统组成

| 模块 | 主要功能 |
|---|---|
| 启灵 S4 机器人 | 提供 26 维关节状态和双臂拖动示教 |
| Pinocchio | 根据 URDF 和 SDK 关节角计算 `T_base_tool` |
| 双手 D405 | 采集眼在手上彩色图像和 AprilTag 位姿 |
| 头部 D435 | 采集眼在手外彩色图像和 AprilTag 位姿 |
| AprilTag | `tag36h11`、ID 10、实际边长 0.107 m |
| 交互采样器 | 人工确认后同步保存关节角、位姿和标注图像 |

### 2.2 坐标系与变换约定

本文使用 `T_A_B` 表示将 B 坐标系中的点变换到 A 坐标系：

```text
p_A = R_A_B * p_B + t_A_B
```

RealSense 彩色光学坐标系为右手坐标系：

- `+X`：图像向右；
- `+Y`：图像向下；
- `+Z`：沿镜头光轴向前。

### 2.3 眼在手上几何模型

眼在手上标定时，相机随工具运动，AprilTag 相对机器人基座静止。对第 `i` 组数据：

```text
T_base_tool,i * T_tool_camera * T_camera_tag,i = T_base_tag
```

待求常量为 `T_tool_camera` 和 `T_base_tag`。多组样本的左侧闭环结果应收敛到同一个 `T_base_tag`。

### 2.4 眼在手外几何模型

头部 D435 标定时，相机相对基座静止，AprilTag 刚性固定在左手并随左臂运动：

```text
T_base_tool,i * T_tool_tag = T_base_camera * T_camera_tag,i
```

待求常量为 `T_base_camera` 和 `T_tool_tag`。该安装方式下，相机与标定板不能同时固定在外部环境中，否则机器人运动无法提供有效手眼约束。

### 2.5 正运动学与关节映射

每组样本记录 26 维 SDK 关节角。Pinocchio 根据启灵 S4 URDF 计算左右工具坐标系在 `base_link` 下的位姿。根据机器人相对运动与视觉相对运动的一致性分析，本次数据采用以下有效映射：

| 机械臂 | 进入 URDF FK 前的符号修正 |
|---|---|
| 右臂 | `right_shoulder_yaw_joint = -1` |
| 左臂 | `left_wrist_roll_joint = -1`，`left_wrist_yaw_joint = -1` |

这些修正是 SDK 反馈数据进入当前 URDF FK 时的坐标映射，只用于运动学和标定计算，不修改机器人底层控制模式。

### 2.6 AprilTag 位姿估计

检测节点根据 AprilTag 四个角点、相机内参、畸变参数和实际边长计算 `T_camera_tag`。每次人工确认采样后保存：

- 26 维关节角、速度和估计力矩；
- Pinocchio 末端位姿 `T_base_tool`；
- AprilTag 视觉位姿 `T_camera_tag`；
- 当前彩色图像及 AprilTag 轮廓和坐标轴标注。

### 2.7 SE(3) 鲁棒优化

对眼在手上数据定义：

```text
T_expected,i = T_base_tag
T_actual,i   = T_base_tool,i * T_tool_camera * T_camera_tag,i
```

对眼在手外数据定义：

```text
T_expected,i = T_base_tool,i * T_tool_tag
T_actual,i   = T_base_camera * T_camera_tag,i
```

两种模式统一使用闭环误差：

```text
E_i = inverse(T_expected,i) * T_actual,i
e_i = Log_SE3(E_i) = [rho_i, phi_i]
```

`rho_i` 为三维平移残差，`phi_i` 为旋转轴与旋转角乘积构成的三维旋转向量。将所有样本的六维残差拼接，使用 `scipy.optimize.least_squares` 和 `soft_l1` 鲁棒损失联合优化两个常变换。

### 2.8 误差评价指标

```text
translation_rmse = sqrt(mean(||rho_i||^2))
rotation_rmse    = sqrt(mean(||phi_i||^2))
```

除 RMSE 外，同时统计最大平移误差和最大旋转误差。这些指标表示 FK、视觉解算和外参组成的闭环一致性，不等于外参相对计量学真值的绝对误差。

### 2.9 实验流程

```text
机械臂拖动到新位姿
        |
        v
松开拖动按键并等待位姿稳定
        |
        v
检查 AprilTag 可见性和图像质量
        |
        v
人工确认后同步保存 q、T_base_tool、T_camera_tag 和标注图像
        |
        v
完成多位姿数据集
        |
        v
关节映射检查 -> Pinocchio FK -> SE(3) 鲁棒优化
        |
        v
输出外参、闭环残差、YAML 文件和 URDF 可视化模型
```

---

## 3 实验结果

### 3.1 数据采集

三组标定实验均采用人工拖动方式改变机械臂位姿。每组标定采集 17 个不同位姿，姿态尽量覆盖不同的位置、距离和旋转方向。

眼在手上实验中，D405 与对应手部保持刚性连接，AprilTag 固定在桌面或外部支架。头部眼在手外实验中，D435 保持固定，AprilTag 刚性固定在左手标定板上。在同一次实验内，相机安装件和标定板安装关系均不得改变。

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=M2RjZTI4ZjNjNzJlZWI2MGRlMDI2YjQ3NDI3YTM0OGVfNDE2NGIxY2E0OGRkYjc1MmJhYzlmODhhNDVlODU1NjRfSUQ6NzY2ODYzMDAyNjY4NjMzNjIzOV8xNzg1NzM5MDg3OjE3ODU4MjU0ODdfVjM)

【图 1 右手 D405 标定样本图像示例】

【图 2 左手 D405 标定样本图像示例】

【图 3 头部 D435 观测左手标定板的样本图像】

### 3.2 右手 D405 眼在手上标定

#### 3.2.1 异常数据与关节映射

首次直接使用 SDK 关节角计算 FK 时，优化器虽然能够数值收敛，但外参平移超出手部安装的合理尺度。机器人相对运动与视觉相对运动对比显示，对 `right_shoulder_yaw_joint` 使用 `-1` 映射后，成对旋转角差中位数由约 11.22° 降低到 0.94°。

完成映射修正后，第 3 组和第 10 组的残差仍显著高于其他样本，最终使用剩余 15 组数据求解。

#### 3.2.2 外参结果

```text
T_RH_tool_camera =

[ 0.6891152054   0.2705679062   0.6722449270  -0.0197078601 ]
[ 0.0364985742   0.9135430764  -0.4051011006   0.0498948230 ]
[-0.7237320553   0.3036973095   0.6196610819   0.0938273481 ]
[ 0.0000000000   0.0000000000   0.0000000000   1.0000000000 ]
```

- 平移：`[-19.708, 49.895, 93.827] mm`
- 四元数 `xyzw`：`[0.197428, 0.388834, -0.065197, 0.897541]`
- RPY：`[26.11°, 46.36°, 3.03°]`

#### 3.2.3 标定与验证误差

| 数据 | 样本数 | 平移 RMSE | 平移最大误差 | 旋转 RMSE | 旋转最大误差 |
|---|---:|---:|---:|---:|---:|
| 标定内部闭环 | 15 | 8.684 mm | 16.806 mm | 1.247° | 2.622° |
| 新标定板位置独立评估 | 5 | 20.798 mm | 36.260 mm | 1.677° | 3.009° |

独立验证采集 10 组新数据，前 5 组用于建立新位置下的 `T_base_tag` 参考，后 5 组用于独立评估。第 9 组验证数据对整体 RMSE 影响较大；诊断性排除该组后，剩余评估数据为 14.561 mm / 1.119°。由于该图像中的 AprilTag 轮廓没有明显异常，不能仅根据残差将其判定为视觉误检。

【图 4 右手 D405 外参在机器人模型中的可视化】

【图 5 右手 D405 独立验证样本】

### 3.3 左手 D405 眼在手上标定

#### 3.3.1 关节映射与样本筛选

左臂 17 组数据直接使用 SDK 关节角时，机器人 FK 相对运动与相机视觉相对运动不一致，原始解的平移 RMSE 为 80.490 mm，旋转 RMSE 为 0.733 rad。对全部样本对进行旋转角不变量检查后，对 `left_wrist_roll_joint` 和 `left_wrist_yaw_joint` 使用 `-1` 映射，机器人运动与视觉运动的相关系数提高到 0.990。

第 1 组和第 7 组残差较大，最终使用 15 组有效数据求解左手外参。

#### 3.3.2 外参结果

```text
T_LH_tool_camera =

[ 0.6454939650  -0.1991191180   0.7373527772  -0.0209877889 ]
[-0.0600361369   0.9492026142   0.3088851883  -0.0745941356 ]
[-0.7614021299  -0.2436513371   0.6007502164   0.0914989107 ]
[ 0.0000000000   0.0000000000   0.0000000000   1.0000000000 ]
```

- 平移：`[-20.988, -74.594, 91.499] mm`
- 四元数 `xyzw`：`[-0.154549, 0.419213, 0.038903, 0.893791]`
- RPY：`[-22.08°, 49.59°, -5.31°]`

#### 3.3.3 标定内部残差

| 有效样本数 | 平移 RMSE | 平移最大误差 | 旋转 RMSE | 旋转最大误差 |
|---:|---:|---:|---:|---:|
| 15 | 13.397 mm | 19.923 mm | 1.995° | 4.097° |

左手相机平移在工具坐标系下呈现 `X` 约 -21.0 mm、`Y` 约 -74.6 mm、`Z` 约 91.5 mm，与左手附近的实际安装尺度一致。

【图 6 左手 D405 外参在机器人模型中的可视化】

### 3.4 头部 D435 眼在手外标定

#### 3.4.1 安装方式与标定数据

D435 固定在机器人头部，AprilTag 标定板刚性固定在左手。通过拖动左臂改变标定板相对头部相机的位姿，共采集 17 组数据，全部用于外参求解。左臂 FK 使用与左手 D405 相同的腕部符号映射。

#### 3.4.2 头部相机外参

```text
T_base_camera =

[ 0.0063112407  -0.7305808888   0.6827969926   0.0240925853 ]
[-0.9988022309   0.0285256704   0.0397541161   0.0044939526 ]
[-0.0485208394  -0.6822300572  -0.7295257892   0.6924775167 ]
[ 0.0000000000   0.0000000000   0.0000000000   1.0000000000 ]
```

- 平移：`[24.093, 4.494, 692.478] mm`
- 四元数 `xyzw`：`[-0.653321, 0.661767, -0.242712, 0.276275]`

四元数对应的旋转矩阵三列分别是相机光学 `+X`、`+Y`、`+Z` 轴在 `base_link` 下的方向。相机光轴为：

```text
Z_camera_in_base = [0.682797, 0.039754, -0.729526]^T
```

因此头部相机主要朝机器人 `+X` 前方，同时向 `-Z` 下方倾斜约 46.85°，水平方向仅偏向 `+Y` 约 3.33°。相机光学中心高度约 0.692 m，水平位置接近机器人中线，与头部安装关系一致。

此外，联合求解得到的标定板固定变换为：

- `T_tool_tag` 平移：`[6.848, -9.300, 162.082] mm`
- `T_tool_tag` 四元数 `xyzw`：`[0.684837, -0.017834, 0.728226, -0.019155]`

标定板中心位于左手工具坐标系 `+Z` 方向约 162 mm，标定板法向基本与手掌法向一致，符合实际固定方式。

#### 3.4.3 标定内部残差

| 样本数 | 平移 RMSE | 平移最大误差 | 旋转 RMSE | 旋转最大误差 |
|---:|---:|---:|---:|---:|
| 17 | 6.500 mm | 9.612 mm | 1.211° | 2.249° |

17 组数据的最大残差没有远高于 RMSE，未发现需要排除的严重离群样本。

#### 3.4.4 独立位姿验证

在头部相机和左手标定板安装关系保持不变的条件下，另外采集 10 组未参与外参求解的左臂位姿。使用原标定的 `T_tool_tag` 对每组数据反算：

```text
T_base_camera,i = T_base_tool,i * T_tool_tag * inverse(T_camera_tag,i)
```

然后将每组 `T_base_camera,i` 与原标定 `T_base_camera` 比较。

| 样本 | 平移误差 | 旋转误差 |
|---:|---:|---:|
| 01 | 32.818 mm | 3.485° |
| 02 | 29.662 mm | 3.024° |
| 03 | 20.117 mm | 2.417° |
| 04 | 20.310 mm | 2.117° |
| 05 | 19.337 mm | 1.715° |
| 06 | 15.809 mm | 1.826° |
| 07 | 17.301 mm | 2.104° |
| 08 | 14.274 mm | 2.325° |
| 09 | 22.666 mm | 3.031° |
| 10 | 18.080 mm | 2.568° |

| 验证指标 | 平移 | 旋转 |
|---|---:|---:|
| 相对原标定外参 RMSE | 21.775 mm | 2.520° |
| 相对原标定外参最大误差 | 32.818 mm | 3.485° |
| 10 组反算位姿相对自身均值的离散 RMSE | 10.321 mm | 1.085° |
| 相对自身均值的最大离散 | 17.487 mm | 1.644° |

验证集反算相机位置的均值为 `[8.651, -3.879, 684.791] mm`，与标定结果 `[24.093, 4.494, 692.478] mm` 存在系统性偏差。10 组结果相对自身均值的离散小于其相对原外参的误差，说明验证集内部重复性较好，但与标定数据集之间仍有姿态相关或系统性差异。

【图 7 头部 D435 独立验证的 10 组 AprilTag 标注图像】

### 3.5 多相机 URDF 可视化

三组标定外参已写入完整机器人 URDF 副本。左右手相机按 D405 的 42 mm × 42 mm × 23 mm 外形显示，头部相机按 D435 的 90 mm × 25 mm × 25 mm 长矩形外形显示。模型的镜头位于光学坐标系 `+Z` 侧，可用于检查相机安装位置和镜头朝向。

【图 8 左手 D405、右手 D405 和头部 D435 在 URDF Visualizer 中的整体可视化】

### 3.6 误差分析

本实验的主要误差来源包括：

1. 机器人关节零位、编码器噪声与传动回差。
2. URDF 连杆长度、关节原点和坐标轴与实机的差异。
3. 手臂柔性、拖动松手后下沉和标定板支架形变。
4. AprilTag 边长误差、打印平面度、像素角点提取和 PnP 位姿噪声。
5. RealSense 内参、畸变模型、曝光和观测距离变化。
6. 机器人状态与相机图像没有硬件同步导致的时间偏差。
7. SDK 关节坐标与 URDF 广义坐标之间的符号、零位或其他有效映射误差。

标定内部残差反映求解器对当次数据集的拟合一致性；独立验证残差还包含新位姿分布、结构变形和姿态相关 FK 误差。本实验没有激光跟踪仪、测量臂或精密装配夹具提供外参真值，因此所有 RMSE 都应表述为闭环一致性指标，不应直接称为外参相对真值的绝对误差。

---

## 4 结论

本实验完成了启灵 S4 机器人右手 D405、左手 D405 和头部 D435 的手眼外参标定，形成了从拖动示教、AprilTag 交互采样、Pinocchio FK、关节映射检查、SE(3) 鲁棒优化到外参 YAML 和 URDF 可视化的完整流程。

| 相机 | 模式 | 外参平移 | 外参四元数 `xyzw` | 标定内部 RMSE |
|---|---|---|---|---|
| 右手 D405 | 眼在手上 | `[-19.708, 49.895, 93.827] mm` | `[0.197428, 0.388834, -0.065197, 0.897541]` | `8.684 mm / 1.247°` |
| 左手 D405 | 眼在手上 | `[-20.988, -74.594, 91.499] mm` | `[-0.154549, 0.419213, 0.038903, 0.893791]` | `13.397 mm / 1.995°` |
| 头部 D435 | 眼在手外 | `[24.093, 4.494, 692.478] mm` | `[-0.653321, 0.661767, -0.242712, 0.276275]` | `6.500 mm / 1.211°` |

右手 D405 的新位姿独立评估结果为 20.798 mm / 1.677°。头部 D435 的 10 组新位姿验证结果为 21.775 mm / 2.520°，验证集内部离散为 10.321 mm / 1.085°。结果表明三组外参在旋转上具有较好的一致性，当前实机完整链路的独立平移一致性约为 2 cm。

当前结果可用于坐标变换、初步目标定位、抓取和视觉伺服实验。若后续任务要求稳定的亚厘米定位精度，还需进一步标定关节零位和机器人几何参数，改善标定板刚度、视觉观测质量与状态时间同步。
